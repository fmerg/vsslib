{
  "name": "vsslib",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "vss": "node cli.js",
    "dev": "npx tsc --watch",
    "test": "jest tests --maxWorkers=1",
    "test:reload": "jest tests --watch --maxWorkers=1 --verbose",
    "test-elgamal:reload": "jest tests/elgamal --watch --maxWorkers=1 --verbose",
    "test-backend:reload": "jest tests/elgamal.backend --watch --maxWorkers=1 --verbose",
    "test-system:reload": "jest tests/elgamal.system --watch --maxWorkers=1 --verbose",
    "test-nizk:reload": "jest tests/elgamal.nizk --watch --maxWorkers=1 --verbose",
    "test-encryption:reload": "jest tests/elgamal.encryption --watch --maxWorkers=1 --verbose",
    "test-key:reload": "jest tests/key --watch --maxWorkers=1 --verbose",
    "test-utils:reload": "jest tests/utils --watch --maxWorkers=1 --verbose",
    "test-bitwise:reload": "jest tests/utils.bitwise --watch --maxWorkers=1 --verbose",
    "test-hash:reload": "jest tests/utils.hash --watch --maxWorkers=1 --verbose",
    "test-polynomial:reload": "jest tests/polynomial --watch --maxWorkers=1 --verbose",
    "test-lagrange:reload": "jest tests/lagrange --watch --maxWorkers=1 --verbose",
    "test-shamir:reload": "jest tests/shamir --watch --maxWorkers=1 --verbose",
    "build": "rm -rf ./build && tsc",
    "docs": "typedoc --out docs src/*.ts"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@noble/curves": "^0.8.3",
    "@types/jest": "^29.2.5",
    "commander": "^10.0.0",
    "ffjavascript": "^0.2.57",
    "jest": "^29.5",
    "noble-ed25519": "^1.2.6",
    "ts-jest": "^29.0.5",
    "typedoc": "^0.23.26",
    "typescript": "^4.9.4"
  }
}
